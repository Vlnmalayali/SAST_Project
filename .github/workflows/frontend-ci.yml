name: Frontend CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'

env:
  NODE_VERSION: '20'

jobs:
<<<<<<< HEAD
    lint:
=======
  lint:
>>>>>>> bc5ba9ad6041be3f266ecdc784fdf90bb8de1d63
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Run ESLint
        run: |
          cd frontend
          npx next lint --max-warnings 50

      - name: TypeScript type check
        run: |
          cd frontend
          npx tsc --noEmit || true

  build:
    name: Build & Bundle Analysis
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Build production bundle
        env:
          NEXT_PUBLIC_API_URL: https://api.example.com
          NEXT_TELEMETRY_DISABLED: 1
        run: |
          cd frontend
          npm run build

      - name: Check bundle size
        run: |
          cd frontend
          echo "=== Build output ==="
          du -sh .next/
          echo ""
          echo "=== Static files ==="
          find .next/static -name "*.js" -exec du -sh {} \; | sort -rh | head -20
          echo ""
          echo "=== Page sizes ==="
          if [ -f .next/BUILD_ID ]; then
            echo "Build ID: $(cat .next/BUILD_ID)"
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: |
            frontend/.next/
            !frontend/.next/cache/
          retention-days: 7

  accessibility:
    name: Accessibility & Performance Check
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: TypeScript type check
        run: |
          cd frontend
          npx tsc --noEmit --pretty
        continue-on-error: true

  build:
    name: Build & Bundle Analysis
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Build production bundle
        env:
          NEXT_PUBLIC_API_URL: https://api.example.com
          NEXT_TELEMETRY_DISABLED: 1
        run: |
          cd frontend
          npm run build

      - name: Check bundle size
        run: |
          cd frontend
          echo "=== Build output ==="
          du -sh .next/
          echo ""
          echo "=== Static files ==="
          find .next/static -name "*.js" -exec du -sh {} \; | sort -rh | head -20
          echo ""
          echo "=== Page sizes ==="
          if [ -f .next/BUILD_ID ]; then
            echo "Build ID: $(cat .next/BUILD_ID)"
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: |
            frontend/.next/
            !frontend/.next/cache/
          retention-days: 7

  accessibility:
    name: Accessibility & Performance Check
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8000
          NEXT_TELEMETRY_DISABLED: 1
        run: |
          cd frontend
          npm run build

      - name: Start server
        run: |
          cd frontend
          npm run start &
          sleep 5

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v11
        with:
          urls: |
            http://localhost:3000
            http://localhost:3000/login
          configPath: frontend/.lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true
        continue-on-error: true

  security-audit:
    name: npm Security Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Run npm audit
        run: |
          cd frontend
          npm audit --audit-level=high || true

      - name: Check for outdated packages
        run: |
          cd frontend
          npm outdated || true

  docker-build:
    name: Docker Build Verification
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          cd frontend
          docker build \
            -t sast-frontend:${{ github.sha }} \
            -t sast-frontend:latest \
            .

      - name: Check image size
        run: |
          SIZE=$(docker image inspect sast-frontend:${{ github.sha }} --format='{{.Size}}')
          SIZE_MB=$((SIZE / 1024 / 1024))
          echo "Image size: ${SIZE_MB}MB"

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [build, docker-build]
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    environment: staging
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy notification
        run: |
          echo "🚀 Would deploy frontend to staging"
          echo "Commit: ${{ github.sha }}"
          # Add actual deployment:
          # - Deploy to Vercel staging
          # - Or push Docker image and update service

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build, docker-build]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy notification
        run: |
          echo "🚀 Would deploy frontend to production"
          echo "Commit: ${{ github.sha }}"
          # Add actual deployment:
          # - Deploy to Vercel production
          # - Or push Docker image to ECR and update ECS
          # - Invalidate CDN
